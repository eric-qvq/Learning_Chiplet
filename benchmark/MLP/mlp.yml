# Phase 1 configuration.
phase1:
  # Process 0
  - cmd: "$BENCHMARK_ROOT/bin/mlp_cu"
    args: ["0", "1"]
    log: "gpgpusim.0.1.log"
    is_to_stdout: false
    env: { PTX_SIM_MODE_FUNC: "1" }
    clock_rate: 1
    pre_copy: "$SIMULATOR_ROOT/gpgpu-sim/configs/tested-cfgs/SM2_GTX480/*"
  # Process 1
  - cmd: "$BENCHMARK_ROOT/bin/mlp_cu"
    args: ["0", "2"]
    log: "gpgpusim.0.2.log"
    is_to_stdout: false
    env: { PTX_SIM_MODE_FUNC: "1" }
    clock_rate: 1
    pre_copy: "$SIMULATOR_ROOT/gpgpu-sim/configs/tested-cfgs/SM2_GTX480/*"
  # Process 2
  - cmd: "$BENCHMARK_ROOT/bin/mlp_cu"
    args: ["0", "3"]
    log: "gpgpusim.0.3.log"
    is_to_stdout: false
    env: { PTX_SIM_MODE_FUNC: "1" }
    clock_rate: 1
    pre_copy: "$SIMULATOR_ROOT/gpgpu-sim/configs/tested-cfgs/SM2_GTX480/*"
  # Process 3
  - cmd: "$BENCHMARK_ROOT/bin/mlp_cu"
    args: ["0", "4"]
    log: "gpgpusim.0.4.log"
    is_to_stdout: false
    env: { PTX_SIM_MODE_FUNC: "1" }
    clock_rate: 1
    pre_copy: "$SIMULATOR_ROOT/gpgpu-sim/configs/tested-cfgs/SM2_GTX480/*"
  # Process 4
  - cmd: "$BENCHMARK_ROOT/bin/mlp_cu"
    args: ["1", "1"]
    log: "gpgpusim.1.1.log"
    is_to_stdout: false
    env: { PTX_SIM_MODE_FUNC: "1" }
    clock_rate: 1
    pre_copy: "$SIMULATOR_ROOT/gpgpu-sim/configs/tested-cfgs/SM2_GTX480/*"
  # Process 5
  - cmd: "$BENCHMARK_ROOT/bin/mlp_cu"
    args: ["1", "2"]
    log: "gpgpusim.1.2.log"
    is_to_stdout: false
    env: { PTX_SIM_MODE_FUNC: "1" }
    clock_rate: 1
    pre_copy: "$SIMULATOR_ROOT/gpgpu-sim/configs/tested-cfgs/SM2_GTX480/*"
  # Process 6
  - cmd: "$BENCHMARK_ROOT/bin/mlp_cu"
    args: ["1", "3"]
    log: "gpgpusim.1.3.log"
    is_to_stdout: false
    env: { PTX_SIM_MODE_FUNC: "1" }
    clock_rate: 1
    pre_copy: "$SIMULATOR_ROOT/gpgpu-sim/configs/tested-cfgs/SM2_GTX480/*"
  # Process 7
  - cmd: "$BENCHMARK_ROOT/bin/mlp_cu"
    args: ["1", "4"]
    log: "gpgpusim.1.4.log"
    is_to_stdout: false
    env: { PTX_SIM_MODE_FUNC: "1" }
    clock_rate: 1
    pre_copy: "$SIMULATOR_ROOT/gpgpu-sim/configs/tested-cfgs/SM2_GTX480/*"
  # Process 8 (CPU via gem5)
  - cmd: "$SIMULATOR_ROOT/snipersim/run-sniper"
    args: ["--", "$BENCHMARK_ROOT/bin/mlp_cpu", "0", "0"]
    log: "sniper.0.0.log"
    is_to_stdout: false
    clock_rate: 1

phase2:
  # Process 0
  - cmd: "$SIMULATOR_ROOT/popnet_chiplet/build/popnet"
    # Chiplet coordinates in the trace go up to (1,4), so the mesh size must be at least 5.
    args: ["-A", "5", "-c", "2", "-V", "3", "-B", "12", "-O", "12", "-F", "4", "-L", "1000", "-T", "10000000", "-r", "1", "-I", "../bench.txt", "-R", "0", "-D", "../delayInfo.txt", "-P"]
    log: "popnet.log"
    is_to_stdout: false
    clock_rate: 1

# File configuration. (Not used yet)
bench_file: "./bench.txt"
delayinfo_file: "./delayInfo.txt"
